#!/usr/bin/env bash
for i in {0..17}; do
    printf '\e[48;5;%sm  \e[0m' "$i" "$i"
    (( (i + 1) % 8 == 0 )) && printf '\n'
done
printf "\n"
for i in {0..17}; do
    printf '\e[38;5;%sm%3s \e[0m' "$i" "$i"

    (( (i + 1) % 8 == 0 )) && printf '\n'
done
printf "\n"
